# Copy this file to `.env` (same directory) and fill in your local Postgres
# credentials. `backend/utils/db/database.py` loads this via python-dotenv.
#
# Format: postgresql+asyncpg://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql+asyncpg://gazemetrics:gazemetrics@localhost:5432/gazemetrics
